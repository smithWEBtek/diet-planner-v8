<h3>Edit Mealname</h3>
<p>
  Use this form only when Admin edits a mealname in the Mealnames table.(breakfast, lunch, dinner, etc.) 
</p>

<%= render partial: 'mealnames/mealname_form', locals: { mealname: @mealname } %>
