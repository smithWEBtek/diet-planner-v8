<div class="index_view">
  <h3>Welcome to DietPlanner!</h3> 
  <h4>Your foods, calories, meals, diets, ups and downs in a supportive volunteer community.</h4>
  <p>"Man who stand on mountain, with mouth open, waiting for Roast Duck to fly in...have very long wait."<i> -- Confucius</i>
  </p>
  <p>“That which is measured, improves.”<i> -- Mom</i>
  </p>
</div>
<hr>

<h4>Random Celebrity Quotes</h4>
<%= render partial: 'quotes/quotes_random', locals: { quotes: @quotes } %>


<div id="new_quote_form"></div>
<hr>
<div class="container">
  <div class="row">
    <div class="col-lg-12">
      <div class="col-sm-3">
        <ul id="celeb_list"></ul>
        <div id="show_celeb"></div>
        <%= form_for(@celeb, url: celebs_path, method: "POST") do |f| %>
          <%= f.label :add_celeb %><br>
          <%= f.text_field :name %><br>
        <%= f.submit %>
        <% end %>
      </div>
      <div class="col-sm-3">
        <ul id="food_list"></ul> 
        <%= form_for(@food, url: foods_path, method: "POST") do |f| %>
          <%= f.label :name %><br>
          <%= f.text_field :name %><br>
          <%= f.label :group %><br>
          <%= f.collection_select :group_id, Group.all, :id, :name %><br>
          <%= f.label :calories %><br>
          <%= f.text_field :cals %><br>
        <%= f.submit %>
        <% end %>
      </div>
      <div class="col-sm-3">
        <ul id="diet_list"></ul> 
        <%= form_for(@diet, url: diets_path, method: "POST") do |f| %>
          <%= f.label :add_diet %><br>
          <%= f.text_field :name %><br>
        <%= f.submit %>
        <% end %>
      </div>
      <div class="col-sm-3">
        <ul id="phrase_list"></ul> 
        <%= form_for(@phrase, url: phrases_path, method: "POST") do |f| %>
          <%= f.label :add_phrase %><br>
          <%= f.text_field :content %><br>
        <%= f.submit %>
        <% end %>
      </div>
    </div>
  </div>
</div>
<hr>
<h4>Group/Foods: has_many relationship in information rendered via JSON and appended to the DOM</h4>
<div class="groups">
  <% @groups.each do |group| %>
    <p data-id="<%= group.id %>"><%= link_to group.name, group_path(group) %></p>  
  <% end %><br>
  <hr>
  <div id="show_group_foods"></div>
  <hr>
</div>
<h4>Index resources rendered using jQuery and Active Model Serialization JSON backend</h4>
<div class="indexes">
  <button>Celebs</button>
    <div id="index_celebs"></div>
  <button>Foods</button>
    <div id="index_foods"></div>
  <button>Diets</button>
    <div id="index_diets"></div>
  <button>Phrases</button>
    <div id="index_phrases"></div>
</div>
<hr>
</div>